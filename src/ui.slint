export component MainWindow inherits Window {
    title: "ftp-slint";
    width: 800px;
    height: 600px;

    VerticalLayout {
        padding: 16px;
        spacing: 8px;
        cross-axis-alignment: center;

        TextInput {
            text: "Hello from Slint!";
            font-size: 24px;
            read-only: true;
        }
    }
}
